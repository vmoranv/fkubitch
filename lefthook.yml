pre-commit:
  parallel: true
  commands:
    pages-build:
      root: "src/pages"
      glob: "src/**/*.{ts,vue,css}"
      run: pnpm exec vite build
    pages-typecheck:
      root: "src/pages"
      glob: "src/**/*.{ts,vue}"
      run: pnpm exec vue-tsc --noEmit --project tsconfig.json
